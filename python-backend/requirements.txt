# Runtime-only dependency list, kept for the devcontainer postCreate step.
# For development install, prefer `pip install -e ".[dev]"` from the repo root
# (uses pyproject.toml to also pull in ruff + pytest).
numpy>=2.0.2
wandas[psychoacoustic]>=0.7.1,<0.8.0
soundfile>=0.12
